[package]
name = "cdc-core"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1"
thiserror = "2"
tokio = { version = "1", features = ["time", "macros"] }
tokio-util = "0.7"
tracing = "0.1"
